doctype html

-var DATA_COUNTRY = 0

-var DATA_CITY = 1 
- //id, name, state, country, coord

-var DATA_COORD = 2
-// lon, lat

-var DATA_GEONAME = 3 
-// geonameid, name, asciiname, alternatenames,latitude, longitude, featureclass, featurecode, countrycode, 
-// cc2, admin1code, admin2code, admin3code, admin4code, population, elevation, dem, timezone, modificationdate

-var DATA_GEOLOCATION = 4
-// place_id, license, osm_type, osm_id, boundingbox, lat, lon, display_name, class, type, importance, icon

-var DATA_BOX = 5
-var DATA_FLAG = 6

html
    head
        title Hello weather
        style(type="text/css").
            .result-box{
                width: max-content;
                height: max-content;
                margin-bottom: 50px;
                border: 1px solid black;
                padding: 10px;
            }
            .result-flag{
                width: 50px;
                height: 50px;
            }
    body
        -for(var x = 0; x < data.length; x++)
            div.result-box
                p=data[x][DATA_CITY].name
                p=data[x][DATA_COUNTRY]
                p=data[x][DATA_COORD].lon
                p=data[x][DATA_COORD].lat
                p=data[x][DATA_GEONAME].admin2code
                p=data[x][DATA_GEOLOCATION].display_name
                div.result-flag
                    img(src=data[x][DATA_FLAG], alt="alt", width="100%", height="100%")
                
        